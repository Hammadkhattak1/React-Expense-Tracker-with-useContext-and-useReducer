import { Fragment } from "react";

const Header = () => {
  return (
    <Fragment>
      <h1 className="display-5 fs-2 text-center fw-bold ">Expense Tracker</h1>
    </Fragment>
  );
};

export default Header;
